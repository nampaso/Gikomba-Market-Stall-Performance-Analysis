## Task 1: Overall Sales Metrics & Extremes
### Approach
We compute aggregate figures using built-in functions `sum()`, `len()`, `max()`, and `min()`. Indices of peak and lowest performance are retrieved using `.index()`, allowing us to resolve the exact day of the week and week index via zero-based integer division.
